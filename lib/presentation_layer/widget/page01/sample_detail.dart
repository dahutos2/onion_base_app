import 'package:flutter/material.dart';

class SampleDetailView extends StatelessWidget {
  const SampleDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('sample_detail_temp'),
    );
  }
}
